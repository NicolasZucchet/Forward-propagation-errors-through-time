# @package _group_

task: "copy"
batch_size: 64
n_samples: 200000
seq_len: 4
bit_width: 1
n_classes: 1  # must match the bit width
waiting_time: 16
# don't touch the following
classification: true
multiple_pred_per_timestep: true
dense_prediction: true
  