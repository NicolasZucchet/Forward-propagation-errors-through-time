[project]
name = "env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flax>=0.10.7",
    "jax[cuda12]>=0.6.2",
    "optax>=0.2.5",
]
