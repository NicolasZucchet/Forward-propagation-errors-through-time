defaults:
  - data: copy
  - _self_

seed: 42

model:
  training_mode: "normal"
  hidden_dim: 64
  precision: "float32"
  T_min_frac: 1.0
  T_max_frac: 2.0

training:
  scheduler: "cosine"
  learning_rate: 3e-4
  log_every_steps: 20
  gradient_clipping: 1.0
